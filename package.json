{
  "name": "altseason-screener",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Screener crypto read-only : scan via GitHub Actions (CMC), dashboard statique sur GitHub Pages.",
  "scripts": {
    "scan": "node scripts/scan.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
